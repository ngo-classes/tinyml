# Arduino CLI binary drop folder

Place the Arduino CLI binary for your OS in this directory:

- Windows: `arduino-cli.exe`
- macOS/Linux: `arduino-cli`

This folder is intentionally configured to ignore everything except this README and the `.gitignore` file, so downloaded binaries do not get committed by accident.
