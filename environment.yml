name: tinyml
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - jupyterlab
  - notebook
  - ipykernel
  - numpy
  - scipy
  - pandas
  - matplotlib
  - pandas
  - pyserial
  - pyyaml
  - click
  - pillow
  - scikit-learn
  - scikit-image
  - seaborn
  - pip:
      # TensorFlow is installed via pip inside the conda environment.
      - tensorflow
      - tensorflow-datasets
      - opencv-python
      - pooch
